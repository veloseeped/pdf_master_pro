# PDF Master Pro

**PDF Master Pro** — это простая и мощная утилита для работы с PDF-документами. Программа позволяет разделять большие файлы на части, склеивать несколько документов в один, менять порядок страниц и выстраивать страницы в обратном порядке (реверс), используя удобный интерфейс с поддержкой Drag-and-Drop (перетаскивания файлов).

---

## 🚀 Возможности

1.  **Разделение документов (Экстрактор блоков):** Вы можете взять один большой PDF и "нарезать" из него несколько новых файлов, указав для каждого файла нужные страницы (как по одной, так и в диапазоне, в том числе - в обратном порядке)
2.  **Объединение (Склейка):** Соберите несколько разных PDF-файлов в один документ.
3.  **Редактирование порядка:** Переставьте страницы местами (вы можете указать только начало диапазона - например, если хотите поменять местами первые две страницы; остальные страницы останутся в изначальном порядке).
4.  **Реверс:** Переверните порядок страниц в документе (с конца в начало) одной кнопкой.

---

## 📖 Инструкция для пользователя

### 1. Извлечение блоков (Вкладка "Экстрактор блоков")
*Идеально для разбивки большого отчета на главы.*

1.  **Выберите файл:** Перетащите исходный PDF в поле "Исходный PDF" или нажмите "Обзор".
2.  **Выберите папку:** Укажите, куда сохранить новые файлы.
3.  **Настройте блоки:**
    * Нажмите **"+ Добавить блок"**.
    * В левом поле укажите страницы (например, `1-3` или `1, 3-5, 7`).
    * В правом поле укажите имя файла (например, `Глава_1`).
    * Создайте столько блоков, сколько файлов вы хотите получить.
4.  Нажмите **"ИЗВЛЕЧЬ ВСЕ БЛОКИ"**.

### 2. Склейка файлов (Вкладка "Склеить PDF")
*Идеально для сборки сканов документов в один файл.*

1.  **Добавьте файлы:** Перетащите несколько PDF-файлов в список или используйте кнопку "Добавить файл".
2.  **Настройте порядок:** Используйте кнопки "Выше" и "Ниже", чтобы расставить документы в нужном порядке.
3.  **Сохранение:** Укажите имя итогового файла в поле "Сохранить как".
4.  Нажмите **"СКЛЕИТЬ"**.

### 3. Редактирование и Реверс (Вкладка "Порядок страниц")
*Для точечной настройки документа.*

* **Изменение порядка:**
    1.  Загрузите файл.
    2.  В поле "Новый порядок" впишите номера страниц через запятую (например, `5, 1-3, 10`). В новый файл попадут указанные страницы в этом порядке; если указана только часть страниц, остальные страницы останутся в изнаальном порядке.
    3.  Укажите путь сохранения и нажмите **"СОЗДАТЬ PDF"**.
* **Реверс (обратный порядок):**
    1.  Просто загрузите файл, укажите путь сохранения и нажмите серую кнопку **"РЕВЕРС СТРАНИЦ"**.



---

## 🛠 Установка и запуск из исходного кода

Если вы хотите запустить программу без сборки в EXE:

**Требования:** Python 3.10+

1.  Установите зависимости:
    ```bash
    pip install pypdf>=3.0.0 tkinterdnd2>=0.3.0
    ```
2.  Запустите программу:
    ```bash
    python main.py
    ```

---

## 📦 Сборка в EXE (Для Windows)

Чтобы создать один исполняемый файл (`.exe`), который можно передавать другим пользователям (даже если у них нет Python), используйте библиотеку `PyInstaller`.


1.  Установите зависимости:
    ```bash
    pip install pyinstaller
    ```
2.  Запустите программу в папке с проектом:
    ```bash
    pyinstaller --noconfirm --onefile --windowed --clean --name "PDF_Master_Pro" --collect-all "tkinterdnd2" main.py
    ```
Готовый файл будет лежать в папке dist/.
